<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:layout_marginBottom="10dp"
    android:background="@drawable/textbox"
    android:backgroundTint="@color/colorPrimary"
    android:padding="6dip">

    <ImageView
        android:id="@+id/ivProfilePhotoRanking"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="10dp"
        android:adjustViewBounds="true" />

    <TextView
        android:id="@+id/tvPositionScoreRanking"
        android:layout_width="fill_parent"
        android:layout_height="26dip"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_toEndOf="@id/ivProfilePhotoRanking"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:text="@string/score_string"
        android:textColor="@android:color/background_dark"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/tvUsernameRanking"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignWithParentIfMissing="true"
        android:layout_above="@id/tvPositionScoreRanking"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@id/ivProfilePhotoRanking"
        android:gravity="center_vertical"
        android:text="@string/username_string"
        android:textColor="@android:color/background_dark"
        android:textSize="16sp"
        android:textStyle="bold" />

</RelativeLayout>